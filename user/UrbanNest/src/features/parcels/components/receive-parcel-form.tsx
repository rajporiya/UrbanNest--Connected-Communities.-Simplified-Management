import { zodResolver } from "@hookform/resolvers/zod"
import { LoaderCircle, PackagePlus, ShieldCheck, UserRound } from "lucide-react"
import { useForm } from "react-hook-form"
import { FormActions, FormSection, RequiredLabel } from "@/components/forms"
import { Button } from "@/components/ui/button"
import { parcelCouriers, parcelPackageTypes, parcelResidents } from "@/features/parcels/data/parcels.mock"
import { receiveParcelSchema, type ReceiveParcelFormValues } from "@/features/parcels/schemas/parcel.schema"
const control = "h-11 w-full rounded-lg border border-input bg-background px-3 text-sm shadow-sm outline-none focus-visible:border-ring focus-visible:ring-2 focus-visible:ring-ring/30"
const ErrorText = ({ message }: { message?: string }) => message ? <p role="alert" className="text-xs text-destructive">{message}</p> : null
export function ReceiveParcelForm({ initialValues, loading = false, onSubmit, onCancel }: { initialValues: ReceiveParcelFormValues; loading?: boolean; onSubmit: (values: ReceiveParcelFormValues) => void | Promise<void>; onCancel: () => void }) { const form = useForm<ReceiveParcelFormValues>({ resolver: zodResolver(receiveParcelSchema), defaultValues: initialValues }); const errors = form.formState.errors; return <form className="space-y-8" noValidate onSubmit={form.handleSubmit(onSubmit)}><FormSection title="Delivery details" description="Record the courier and tracking information exactly as shown on the package." icon={<PackagePlus />} columns={2} divider><label className="space-y-1.5 text-sm font-medium"><RequiredLabel required>Courier</RequiredLabel><select className={control} {...form.register("courierName")}><option value="">Select courier</option>{parcelCouriers.map((item) => <option key={item}>{item}</option>)}</select><ErrorText message={errors.courierName?.message} /></label><label className="space-y-1.5 text-sm font-medium"><RequiredLabel required>Tracking number</RequiredLabel><input className={control} {...form.register("trackingNumber")} /><ErrorText message={errors.trackingNumber?.message} /></label><label className="space-y-1.5 text-sm font-medium"><RequiredLabel required>Package type</RequiredLabel><select className={control} {...form.register("packageType")}>{parcelPackageTypes.map((item) => <option key={item}>{item}</option>)}</select></label></FormSection><FormSection title="Resident and custody" description="Choose the recipient and identify the guard accepting custody." icon={<UserRound />} columns={2} divider><label className="space-y-1.5 text-sm font-medium"><RequiredLabel required>Resident</RequiredLabel><select className={control} {...form.register("residentId")}><option value="">Select resident</option>{parcelResidents.map((item) => <option key={item.id} value={item.id}>{item.name} · {item.tower} {item.flat}</option>)}</select><ErrorText message={errors.residentId?.message} /></label><label className="space-y-1.5 text-sm font-medium"><RequiredLabel required>Received by</RequiredLabel><input className={control} placeholder="Security guard name" {...form.register("receivedBy")} /><ErrorText message={errors.receivedBy?.message} /></label><label className="space-y-1.5 text-sm font-medium sm:col-span-2"><span>Handling notes</span><textarea rows={4} className={`${control} h-auto py-3`} {...form.register("notes")} /><ErrorText message={errors.notes?.message} /></label></FormSection><div className="flex items-start gap-3 rounded-xl border border-primary/20 bg-primary/5 p-4 text-sm"><ShieldCheck className="mt-0.5 size-5 shrink-0 text-primary" /><p className="text-muted-foreground">Receiving the parcel records a custody timestamp. Notify the resident from the parcel details page.</p></div><FormActions className="border-t"><Button type="button" variant="outline" disabled={loading} onClick={onCancel}>Cancel</Button><Button type="submit" disabled={loading}>{loading ? <LoaderCircle className="animate-spin" /> : <PackagePlus />}{loading ? "Receiving..." : "Receive parcel"}</Button></FormActions></form> }
