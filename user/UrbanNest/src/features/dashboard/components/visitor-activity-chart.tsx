import { Bar, BarChart, CartesianGrid, Legend, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts"
import { ContentCard } from "@/components/common/content-card"
import { EmptyState } from "@/components/feedback/empty-state"
import type { VisitorActivityPoint } from "@/features/dashboard/types/dashboard.types"
export function VisitorActivityChart({ data, loading = false }: { data: VisitorActivityPoint[]; loading?: boolean }) { return <ContentCard title="Visitor activity" description="Expected and checked-in visitors over the last seven days" loading={loading}><div role="img" aria-label="Seven day expected and checked-in visitor activity chart" className="h-72">{data.length ? <ResponsiveContainer width="100%" height="100%"><BarChart data={data}><CartesianGrid strokeDasharray="3 3" stroke="var(--border)" /><XAxis dataKey="day" /><YAxis allowDecimals={false} /><Tooltip /><Legend /><Bar dataKey="expected" name="Expected" fill="var(--chart-2)" radius={[4, 4, 0, 0]} /><Bar dataKey="checkedIn" name="Checked in" fill="var(--chart-4)" radius={[4, 4, 0, 0]} /></BarChart></ResponsiveContainer> : <EmptyState compact title="No visitor data" />}</div></ContentCard> }
