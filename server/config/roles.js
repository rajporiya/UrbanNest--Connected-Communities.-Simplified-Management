export const ROLES = {
  COMMITTEE_HEAD: "Committee Head",
  COMMITTEE_MEMBER: "Committee Member",
  RESIDENT: "Resident",
  SECURITY_GUARD: "Security Guard",
}

export const ALL_ROLES = Object.values(ROLES)
export const DEFAULT_ROLE = ROLES.RESIDENT

export default ROLES
